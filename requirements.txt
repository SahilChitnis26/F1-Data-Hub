# Python 3.11+
# API server (serves React build + API on :8000)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Data & analysis
pandas>=2.0.0
numpy>=1.24.0

# HTTP client (Ergast, FastF1)
requests>=2.28.0

# Lap-level telemetry (deep analysis, execution score)
fastf1>=3.0.0

# Track map / circuit visualization (replay, track layout)
# trackmap is not on PyPI; install from your Git repo, e.g.:
#   pip install "trackmap @ git+https://github.com/<org>/trackmap.git"
# Dependencies typically needed for track map plotting (e.g. with FastF1):
matplotlib>=3.7.0

# Development / testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
