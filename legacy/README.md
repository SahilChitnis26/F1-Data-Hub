# Legacy UI (reference only)

This folder contains the original vanilla HTML + JS dashboard for reference only.

- **dashboard.html** – Legacy single-page dashboard (Chart.js, DOM-based controls). Not served at runtime. The React app in `frontend/` is the only UI.
